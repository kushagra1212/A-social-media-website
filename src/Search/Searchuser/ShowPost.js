import Container from '../../Profile/Container/Container';
const ShowPost = ({ username, toDelete }) => {
  return <Container username={username} toDelete={false} />;
};

export default ShowPost;
